<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    mc:Ignorable="d">

    <!-- GRAPHITE: the sidebar is laid out HORIZONTALLY as a top strip (rail moved
         to the top). MainWindow docks it Top; the items run left-to-right. This
         frees the entire left edge for the console explorer. -->
    <Style TargetType="{x:Type Sidebar}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Sidebar}">
                    <Border BorderThickness="0" Background="{DynamicResource DarkBackgroundBrush}"
                            x:Name="BorderContentHolder" Margin="{TemplateBinding Padding}">
                        <DockPanel x:Name="DockContentHolder" LastChildFill="True">
                            <Border x:Name="PART_ElemMainMenu" Background="Transparent"
                                    Width="40" Height="40" Margin="6,1,4,1" VerticalAlignment="Center"
                                    DockPanel.Dock="Left" Padding="5" Cursor="Hand"
                                    WindowChrome.IsHitTestVisibleInChrome="True">
                                <Image Source="{ThemeFile 'Images/applogo_dark.png'}"
                                       RenderOptions.BitmapScalingMode="Fant" />
                            </Border>
                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                                          DockPanel.Dock="Left" CanContentScroll="True"
                                          Margin="0,0,4,0" VerticalAlignment="Center" x:Name="ScrollSidebarItems">
                                <StackPanel x:Name="PART_PanelSideBarItems" Orientation="Horizontal" VerticalAlignment="Center" />
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
