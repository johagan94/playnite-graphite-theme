<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Style TargetType="{x:Type SearchBox}">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type SearchBox}">
                    <!-- Borderless + transparent: blends fully into the dark top bar, no field outline / white line. -->
                    <Border Background="Transparent" BorderThickness="0">
                        <Grid>
                            <ContentControl x:Name="PART_SeachIcon"
                                            ContentTemplate="{DynamicResource SearchTextIconTemplate}"
                                            VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8,0,0,0"
                                            Foreground="{DynamicResource TextBrushDarker}" />
                            <TextBox x:Name="PART_TextInpuText" Background="Transparent" BorderThickness="0"
                                     Foreground="{DynamicResource TextBrush}" CaretBrush="{DynamicResource TextBrush}"
                                     FocusVisualStyle="{x:Null}"
                                     VerticalContentAlignment="Center" VerticalAlignment="Stretch" Padding="30,0,25,0">
                                <!-- Minimal template: no border/highlight of its own (kills the white focus border
                                     + the layout jump on focus); the wrapping Border is the field. -->
                                <TextBox.Template>
                                    <ControlTemplate TargetType="TextBox">
                                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                                      VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden" />
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                            <ContentControl x:Name="PART_ClearTextIcon"
                                            ContentTemplate="{DynamicResource ClearTextIconTemplate}"
                                            FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,5,0"
                                            Foreground="{DynamicResource TextBrushDarker}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>